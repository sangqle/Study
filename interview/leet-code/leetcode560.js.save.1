const arr = [1, 4, 40, 6, 0, 19];
const k = 51;
let left = 0;
let right = 0;
let maxCount = 0;
let sum = 0;
while(left <= right){
  sum += arr[right];
  if(sum < k) {
{1, 4, 45, 6, 0, 19}
    right++;
  } else if(sum >= k) {
    maxCount = right - left + 1;
    sum -= arr[left];
    left++;
  }
}
